pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello World"
            }
        }
    }
    post {
        always {
            echo "Always"
        }
        success {
            echo "Success"
        }
        failure {
            echo "Failure"
        }
    }
}
